<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Principais Histórias</title>
    <%- include('../partials/head') %> 
    <link rel="stylesheet" type="text/css" href="/css/story.css">
</head>
<body>
    <%- include('../partials/header', {user: user}); %>
    <div class="ex_title">
        <div class="title">
            <h1 id="history_title">Dê uma olhada nas nossas histórias</h1>
        </div>
    </div>

    <div id="content">
        <p>As histórias que temos disponíveis no momento são: </p>

        <div class="row">
        <% stories.forEach(function(story){ %>
            <div class="col-md-6">
                <div class="card" style="margin-bottom: 20px;">
                    <div class="card-header">
                        ❤️ <%- story.likes %>

                        <h5 class="card-title" style="font-family:'Lucida Handwriting Italic'; display: inline-block; width: 70%;"><%- story.Titulo %> </h5>

                        <button class="material-icons collapse_button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%- story._id %>" aria-expanded="false" aria-controls="collapse<%- story._id %>">arrow_drop_down</button>

                        <br>
                        
                        <span class="description">Feito por: <%- story.owner_name %></span>
                        
                    </div>
                    <div class="collapse" id="collapse<%- story._id %>">
                        <div class="card-body text-center">
                            <a href=""></a>
                            <p class="card-text text-break" style="text-indent:0px; text-align: justify;">
                                "<%-`${story.Escolhas[0].História.slice(0, 100)}${story.Escolhas[0].História.length > 100 ? "..." : "" }`%>"
                            </p>

                            <a href="/jogar_historia/<%- story._id %>/"><img src="/images/play.png" alt="Jogar História" class="play_button"></a>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
        </div>
    </div>
</body>
</html>