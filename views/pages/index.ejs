<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Principais Histórias</title>
    <%- include('../partials/head') %> 
    <link rel="stylesheet" type="text/css" href="/css/story.css">
</head>
<body>
    <%- include('../partials/header', {user: user}); %>
    <div class="ex_title">
        <div class="title">
            <h1 id="history_title">Dê uma olhada nas nossas histórias</h1>
        </div>
    </div>

    <div id="content">
        <p>As histórias que temos disponíveis no momento são: </p>

        <div class="row">
        <% stories.forEach(function(story){ %>
            <div class="col-md-6">
                <div class="card" style="margin-bottom: 20px;">
                    <div class="card-header">
                        ❤️ <%- story.likes %>
                    </div>
                    <div class="card-body text-center">
                        <h5 class="card-title" style="font-family:'Lucida Handwriting Italic';"><%- story.Titulo %></h5>
                        <p class="card-text text-break" style="text-indent:0px; text-align: justify;">
                            "<%-`${story.Escolhas[0].História.slice(0, 200)}${story.Escolhas[0].História.length > 200 ? " ..." : "" }`%>"
                        </p>

                        <a href="/jogar_historia/<%- story._id %>/" class="btn btn-primary">Jogar História</a>
                    </div>
                </div>
            </div>
        <% }); %>
        </div>
    </div>
</body>
</html>